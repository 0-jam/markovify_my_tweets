# RNN (Recursive Neural Network) nodes
DEFAULT_PARAMETERS = {
    "embedding_dim": 256,
    "units": 2048,
    "batch_size": 64
}

TEST_MODE_PARAMETERS = {
    "embedding_dim": 4,
    "units": 16,
    "batch_size": 128
}

BENCHMARK_PARAMETERS = {
    "embedding_dim": 256,
    "units": 1024,
    "batch_size": 64
}
